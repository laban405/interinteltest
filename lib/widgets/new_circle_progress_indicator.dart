import 'package:flutter/material.dart';

Widget newCircularProgressIndicator(context) {
  return Center(
    child: CircularProgressIndicator(
      color: Theme.of(context).primaryColor,
    ),
  );
}
